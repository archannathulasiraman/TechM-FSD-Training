package utility;

public class UtilityClass {
	 public static int findLength(int number) {
	        return String.valueOf(Math.abs(number)).length(); // Convert to string and find the length
	    }
	

}
